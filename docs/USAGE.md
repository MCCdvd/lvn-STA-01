# Usage

```bash
python -m src.cli.main backtest --ticker UCG
python -m src.cli.main optimize --data-dir data/
python -m src.cli.main walk-forward --ticker UCG
python -m src.cli.main visualize --ticker UCG
python -m src.cli.main report --results-dir output/
python -m src.cli.main validate --data-dir data/
```
